import React from 'react';
import type { ComponentProps } from '../../registry.js';
export interface ThreadListProps {
    showSearch?: boolean;
    showNewButton?: boolean;
    showFolders?: boolean;
    showDelete?: boolean;
}
export declare function AssistantThreadList({ instance, children }: ComponentProps): React.ReactElement;
export declare namespace AssistantThreadList {
    var displayName: string;
}
//# sourceMappingURL=ThreadList.d.ts.map